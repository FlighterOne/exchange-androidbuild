{
  "name": "exchange-android",
  "version": "1.0.0",
  "private": true,
  "description": "ExChange Android (Capacitor WebView)",
  "scripts": {
    "build:web": "node -e \"console.log('www ready')\"",
    "cap:sync": "npx cap sync android",
    "build:apk": "cd android && chmod +x gradlew && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/app": "^6.0.2",
    "@capacitor/browser": "^6.0.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}